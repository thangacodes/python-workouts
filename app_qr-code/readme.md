```bash
This repository allows you to create custom QR codes for your applications easily.

Pre-requisites:
Before generating QR codes, ensure your machine has the following installed:
Python 3
Python virtual environment (venv) enabled

Pre-requisites:
Python3 should be installed and exist
Python3 venv needs to be enabled
python3 -m venv app
source app/bin/activate
pip install qrcode

Usage:
Once your environment is setup and dependencies are installed, you can start generating QR codes.
```
